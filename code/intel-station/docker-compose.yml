services:
  intel-station:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./src:/app/src
      - ./data:/app/data
      - ./assets:/app/assets
      - ./intel:/app/intel
    env_file:
      - .env
    restart: unless-stopped
